<div class="d-flex justify-content-between align-items-center">
  <nav aria-label="breadcrumb">
    <ol class="breadcrumb">
      <li class="breadcrumb-item"><%= link_to @chat.challenge.name, challenge_path(@chat.challenge)  %></li>
      <li class="breadcrumb-item active" aria-current="page"><%= @chat.title %></li>
    </ol>
  </nav>
  <nav aria-label="breadcrumb">
    <ol class="breadcrumb">
      <li class="breadcrumb-item"><%= link_to 'All Chats', challenges_path  %></li>
      <li class="breadcrumb-item"><%= link_to 'New Chat', challenge_chats_path(@challenge), data: { turbo_method: :post }  %></li>
    </ol>
  </nav>
</div>
<% if Rails.env.development? %>
  <span class="float-end">
    Input tokens: <%= @input_tokens %> / Output tokens: <%= @output_tokens %>
  </span>
<% end %>
<div id="chat_title">
  <h1><%= @chat.title %></h1>
</div>
<%= turbo_stream_from @chat %>
<div id="messages">
  <%= render 'messages/messages', messages: @chat.messages.visible.order(:created_at) %>
</div>
<div id="new_message">
  <%= render "messages/form", chat: @chat, message: @message %>
</div>
<%# <a href="" data-action="click->controller#action"></a> %>
<%# Controller -> it'll have it's own file, wrap it around all the HTML you need %>
<%# Action -> addEventListener X, click->controller#action %>
<%# Targets -> querySelector X %>
